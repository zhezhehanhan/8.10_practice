# 8.10 作业：环形队列、位运算封包与解包

> 本作业只使用板载 LED，不依赖蜂鸣器和 TIM3。
> 开始前先阅读 `README.md` 中的指针、数组、位运算、环形队列章节。

## 一、作业说明

种子工程已提供 `Core/`、`Drivers/`、`hardware/` 和 `MDK-ARM/`。本作业必做三道，选做两道：

1. 环形队列实现；
2. 位运算封包（含校验和）；
3. 位运算解包（含包头与校验和校验）；
4. 了解 CAN 协议帧结构（选做）；
5. 用函数指针表封装状态机（选做）。

代码写在 `common/src/command_pack_queue.c` 中，`main.c` 的调用框架已写好。

## 二、已经提供的部分

- `common/inc/command_pack_queue.h`：`command_packet`（4 字节，含校验和）、`packet_queue` 类型及所有函数声明，无需修改；
- `common/src/command_pack_queue.c`：函数骨架，`TODO` 标记处需要补全；`command_led_execute` 已封装好；
- `Core/Src/main.c`：队列初始化、封包入队、主循环出队解包的调用框架已写好；
- `hardware/inc/led.h` 和 `hardware/src/led.c`：`led_on(led_mask)` / `led_off(led_mask)` 已实现，支持按位或组合多颗 LED；
- 编译、下载、烧录流程已经可用。

## 三、需要自己做的事

- 把 `common/src/command_pack_queue.c` 和 `common/inc` 加入 Keil 工程（参考 8.7 作业 3 的流程）；
- 按题目要求补全 `.c` 文件中的函数；
- 每完成一题编译下载确认现象。
- .c文件中会有注释提示

---

### 题目 1：环形队列实现

模仿 README 中环形队列的写法，手写补全 `packet_queue` 的 `init`、`push`、`pop`、`is_empty`、`is_full` 五个函数，头文件中的类型和声明已给。队列元素类型是 `command_packet`，其余逻辑与 README 示例一致。

---

### 题目 2：位运算封包（含校验和）

补全 `command_pack_create(pkt, blink_count, led_mask)`，用移位和按位或填入：

- `header[0]`、`header[1]` 分别填入 `HEADER_HIGH_BYTE`、`HEADER_LOW_BYTE`（拼成 16 位即 `0x0A5A`）
- `cmd` 为命令字：**高 4 位**填入 `blink_count`（闪烁次数，0~15），**低 4 位**填入 `led_mask`（LED 掩码，bit0~bit3 对应 LED1~LED4）
- `checksum` = `header[0] + header[1] + cmd` 的低 8 位

```c
command_packet pkt;
command_pack_create(&pkt, 3U, LED_MASK_LED1);
// pkt = {0x0A, 0x5A, 0x31, 校验和}   （0x31 = (3<<4) | 0x01，LED1 闪烁 3 次）
```

要求：

1. `cmd` 必须用左移 `<<` 和按位或 `|` 拼装，不能直接传值赋值；
2. `PACKET_CHECKSUM` 宏已定义在头文件中。

---

### 题目 3：位运算解包（含校验）

补全 `command_pack_unpack(pkt, blink_count, led_mask)`，从协议包中解出命令并校验：

1. **包头校验**：用位运算将两个 `header` 字节拼成 `uint16_t`，与 `HEADER_WORD`（`0x0A5A`）比较；
2. **校验和校验**：用 `PACKET_CHECKSUM` 宏重新计算，与 `pkt->checksum` 比较；
3. 两项校验均通过则用移位和掩码从 `cmd` 中解出闪烁次数与 LED 掩码，写入输出参数并返回 `true`；任意一项失败返回 `false`。

`main.c` 中的调用流程已写好：

```c
// USER CODE 2：初始化队列，封包入队
packet_queue_init(&cmd_queue);
command_packet test_pkt;
command_pack_create(&test_pkt, 3U, LED_MASK_LED1 | LED_MASK_LED2);  // LED1、LED2 闪烁 3 次
packet_queue_push(&cmd_queue, &test_pkt);

// 主循环：出队 -> 解包 -> LED 执行
command_packet recv_pkt;
uint8_t blink_count, led_mask;
if (packet_queue_pop(&cmd_queue, &recv_pkt))
{
    if (command_pack_unpack(&recv_pkt, &blink_count, &led_mask))
    {
        command_led_execute(blink_count, led_mask);  // 包命令执行函数,已封装
    }
}
```

可自行修改 `USER CODE 2` 中 `command_pack_create` 的参数来测试不同的闪烁效果。

---

### 题目 4：了解 CAN 协议帧结构（选做）

查阅资料，了解经典 CAN 2.0 标准数据帧的字段结构，对比本作业的简易协议包，回答以下问题：

1. CAN 标准帧包含哪些字段？各占多少位？
2. CAN 帧的 CRC 校验与本作业的校验和有什么异同？
3. 如果让你设计一个机器人内部 CAN 总线上的电机控制帧，你会怎么划分 ID 和数据段的用途？

不要求写代码，将答案写在 Markdown 学习记录中即可。

---

### 题目 5：用函数指针表封装状态机（选做）

在 `user/` 目录下新建文件，用函数指针表实现一个通用的有限状态机框架。

参考思路：

```c
typedef void (*state_func)(void);

static const state_func state_table[] = {
    [STATE_IDLE]    = state_idle_run,
    [STATE_RUNNING] = state_running_run,
    [STATE_ALARM]   = state_alarm_run,
    [STATE_COUNT]   = NULL            // 哨兵
};
```

要求：

1. 用枚举定义至少 3 个状态，每个状态对应一个运行函数；
2. 状态切换在运行函数内部完成（修改一个全局状态变量）；
3. 主循环只做一件事：查表 -> 调函数，不写 `switch`；
4. 用 LED 的不同闪烁模式体现不同状态（如待机=全灭、运行=流水灯、报警=快闪）；
5. 将文件加入 Keil 工程，编译下载确认现象。

提示：参考 README 中"函数指针与命令表"一节的思想，把"命令码 → 处理函数"换成"状态 → 运行函数"即可。

---

## 四、提交要求

- 每完成一个题目提交一次 Git；
- 提交信息写清楚，例如 `feat: complete packet queue`、`feat: add pack function`、`feat: add unpack function`；
- 最终代码能编译、下载，LED 按命令参数闪烁；
- 写一份 Markdown 学习记录，简单记录调试中遇到的问题和解决方法。

---
